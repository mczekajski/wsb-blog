export enum Routes {
    HEROKU = 'https://wsb-blog-backend.herokuapp.com/',
    LOCAL = 'http://localhost:80/',
    BASE = LOCAL,
    LOGIN = 'user/login',
    POSTS = 'posts',
    SORT_BY_DATE = '?_sort=postDate&_order=desc',
}